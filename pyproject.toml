[project]
name = "dspy-bedrock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3[crt]>=1.40.1",
    "dspy-ai>=2.6.27",
    "pydantic>=2.11.7",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "ruff>=0.12.7",
]
